{
  "name": "athena",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix frontend && python -m pip install -r backend/requirements.txt",
    "dev": "concurrently --kill-others --names backend,frontend --prefix-colors cyan,white \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
